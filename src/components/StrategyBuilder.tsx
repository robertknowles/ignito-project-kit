import React from 'react';

interface StrategyBuilderProps {
  profileOnly?: boolean;
  propertyOnly?: boolean;
}

export function StrategyBuilder({ profileOnly, propertyOnly }: StrategyBuilderProps) {
  return <div>StrategyBuilder Component</div>;
}